# MethArCT 依赖文件 (LoongArch64)

# 核心依赖
biopython>=1.79
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
pyyaml>=5.4
tqdm>=4.60.0
requests>=2.25.0
joblib>=1.0.0
loguru>=0.6.0
scipy>=1.7.0

# 以下为龙芯架构可能需要从源码构建的包
# 如果 pip install 失败，请使用 conda 安装或从源码编译
# numpy       - conda-forge 已支持 loongarch64
# pandas      - conda-forge 已支持 loongarch64
# scipy       - conda-forge 已支持 loongarch64
# scikit-learn - conda-forge 已支持 loongarch64
# biopython   - 纯 Python + 少量 C 扩展，支持源码编译
